{
  "name": "@zurb/helio-cli",
  "version": "0.1.0",
  "description": "CLI for the Helio Public API",
  "type": "module",
  "bin": {
    "helio-cli": "./dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@helio-app/sdk": "^0.1.0",
    "commander": "^12.0.0"
  }
}
